:subtitle: Hoare Logic
:docinfo: shared
:relfileprefix: ../
include::{relfileprefix}README.adoc[tag=preamble]

:relfileprefix:

[source,factor]
----
: add5 ( a -- b ) 5 + ;
----

<1> in forth everything between the parenthesis are just comments
<2> in factor they are sort-of type checked, just probably not the way you're used to.

[source,factor]
----
: add5 ( num -- num ) 5 + ;
----

<3> adding subtypes
<4> what if we could have refinement types? ie add predicates/conditions/constraints to the types?

[source,factor]
----
: add5 ( num -- num > 5 ) 5 + ;
----
